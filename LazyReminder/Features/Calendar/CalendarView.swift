import SwiftUI

struct CalendarView: View {
    var body: some View {
        NavigationStack {
            Text("Calendar")
                .navigationTitle("Calendar")
        }
    }
}

#Preview {
    CalendarView()
}
